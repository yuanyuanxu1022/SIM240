# 阶段一细网格质量与通量诊断报告

> 持久任务更新：`dx2p5_persistent_20260905` 已通过 `setsid` 启动，当前状态需以 `pgrep -af dx5Integrated` 和 `dx2p5_persistent.log` 为准；未达到原收敛判据前不启动 dx=1.25 nm。

状态核对更新：2.5 nm 日志未打印明确 tracer 提示，持久启动也未保存退出码；依据程序代码仅有 tracer break 或达到最大时间两条路径，且最终 step=15423 远低于最大步数并写出 CSV，故记为“推断达到 tracer 收敛，结束状态待确认”。1.25 nm 任务仍在运行，最新约 step 1800，尚未达到收敛判据。

最新核对（2026-09-05）：PID 文件中的旧 PID `1975862` 已不存在，不能作为进程证据；2.5 nm 实际任务已由 `mpirun`/`dx5Integrated` 正常结束，日志最后记录 step 15423，随后写出诊断 CSV。该任务未出现 NaN/Inf，且因在最大步数前结束而判定达到原 tracer 收敛条件。按顺序已启动独立的 dx=1.25 nm 持久任务 `dx1p25_persistent_20260905`（RESOLUTION=192），当前 `mpirun` 和求解进程仍存在，日志为 `dx1p25_persistent.log`，CSV 已开始写入；尚未达到收敛判据。

最新状态：dx=2.5 nm 的持久任务已正常结束，诊断 CSV 最后一条为 step 15423、物理时间 1.6065625e-08 s，B_yy=102.37252398 nm²；质量最大相对漂移 3.39e-14，最大密度偏差 1.11e-12，两截面 Qy 相对差异为 0（输出精度内）。随后已按顺序使用 `setsid` 启动 dx=1.25 nm：`mpirun -np 1 ./dx5Integrated --run-id dx1p25_persistent_20260905 --RESOLUTION 192`，当前运行中，日志为 `dx1p25_persistent.log`。

## 体力与旧 Qy 差异核对

当前独立 audit 与原案例使用 `a_y=1e8 m/s²`、`rho=1000 kg/m³`、`nu=1e-6 m²/s`、`A_cell=600 nm×65 nm`，并按
`B_yy=mu*(Qy/A_cell)/(rho*a_y)` 计算。因此本次 dx=5 nm 的
`Qy=4.38502885e-16 m³/s` 与旧记录 `4.38502885e-18 m³/s` 相差 100 倍，原因是体力相差 100 倍（旧记录对应约 `a_y=1e6 m/s²`）；两者给出的 B_yy 仍约为 112.4366 nm²，不能混用 Qy 数值。

## dx=5 nm 正式结果

run-id：`dx5_formal_20260905`，单 MPI rank。收敛于 step 3909，物理时间 `1.62875e-08 s`。

- `B_yy=112.4366373 nm²`
- 流体节点 32928，体积 `4.1160e-21 m³`
- 流体质量最大绝对相对漂移 `7.16e-13`
- 流体区域最大密度偏差 `1.806e-11`
- y=60/120 nm 两截面 Qy 均 `4.38502885e-16 m³/s`，相对差异为 0（输出精度内）

## dx=2.5 nm

run-id：`dx2p5_formal_20260905`。运行达到外部 300 s 安全时间上限，被安全终止，**未达到原收敛判据**；保留中间 CSV 和日志，不报告为正式收敛值。

截至最后完整记录 step 4431（`4.615625e-09 s`）：

- `B_yy=102.3723422 nm²`（中间值，接近旧参考 102.372523 nm²）
- 流体节点 251424，体积 `3.9285e-21 m³`
- 流体质量最大绝对相对漂移 `3.39e-14`
- 最大密度偏差 `1.11e-12`
- 两截面 Qy 均约 `3.99252134e-16 m³/s`，相对差异 0（输出精度内）

## dx=1.25 nm

本轮未启动。由于 dx=2.5 nm 已达到安全时间上限且尚未收敛，按“保留中间结果、不得伪造最终值”原则停止，避免在未完成诊断接入验证的情况下继续占用长时间计算。

## 网格外推

不能用本轮数据计算正式三网格 Richardson/GCI：缺少 dx=1.25 nm 的本次实际收敛结果，且 dx=2.5 nm 仅为中间状态。已有旧三网格 B_yy（112.436637、102.372523、97.616427 nm²）可作历史趋势参考，但不应与本次不完整诊断结果拼接为正式 GCI。

## 文件与保护范围

完整日志为 `formal_run.log`、`dx2p5_run.log`；诊断 CSV 位于各自 `output/<run-id>/`。未修改原阶段一、阶段二、阶段三目录或 `../tmp/`，未覆盖旧结果。

## 结论

dx=5 nm 的真实质量/通量诊断已完成并复现粗网格 B_yy；dx=2.5 nm 仅完成未收敛中间运行；dx=1.25 nm 未运行。因此阶段一三网格正式质量与通量验证尚未完成，不能据此宣称三网格守恒认证或严格网格无关。

## 后续持久运行尝试（本轮）

针对上次 `timeout 300`，已确认限制来自外部启动命令而非程序参数：程序默认 `MAX_PHYS_T=3e-7 s`，收敛判据未修改。随后使用无 `timeout` 的持久会话重新启动 `dx2p5_persist_20260905`，日志持续推进至约 step 13000（约 `1.35e-08 s`），但在本轮交互资源窗口结束前尚未触发原收敛判据，遂安全中止并保留日志和 CSV。该状态标记为“外部停止/未收敛”，不是数值失败。因 dx=2.5 nm 尚未正式收敛，dx=1.25 nm 未启动。
