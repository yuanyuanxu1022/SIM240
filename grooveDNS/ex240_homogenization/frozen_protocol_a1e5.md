# h=65 nm 单点等效化冻结协议 v2（a=1e5 m/s²）

建立原因：首轮 `a=1e8 m/s²` 的 X 短测产生 `max|rho-1|=9.91e-5`，未通过原先已冻结的 `1e-7` 短测门槛。该失败完整保留；没有放宽任何验收阈值。

- 唯一方案变化：X/Y 驱动由 `1e8` 降至 `1e5 m/s²`，以降低弱可压缩启动扰动并保持低 Re、低 Mach 线性响应测试。
- 网格、几何、dt、tau、100/30000 步限制、1000 步窗口、全部收敛及验收阈值均与 `frozen_protocol.md` 相同。
- 新 run-id 含 `a1e5`，不得覆盖首轮失败。
